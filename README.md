# dbscan_playing_around
Studying dbscan by playing with it.

We use make_moons dataset from sklearn to experiment with DBSCAN and see how it can succesfully cluster non-convex clusters where k-means would fail
